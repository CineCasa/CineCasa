// TROCAR PARA SUA URL DO WORKER SE NECESSÁRIOconst API_BASE = "https://SEU-WORKER.workers.dev";let scrollBeforeOpen = 0;// movie atual selecionadolet currentTmdbId = null;let currentTitle = null;let currentDesc = null;let currentTrailerUrl = null;// overlay elementsconst player = document.getElementById("tiktokPlayer");const backBtn = document.getElementById("backBtn");const soundBtn = document.getElementById("soundBtn");const watchNowBtn = document.getElementById("watchNowBtn");const detailsBtn = document.getElementById("detailsBtn");const bgVideo = document.getElementById("bgVideo");const mainVideo = document.getElementById("mainVideo");const playerTitle = document.getElementById("playerTitle");const playerDesc = document.getElementById("playerDesc");function lockScroll() {  scrollBeforeOpen = window.scrollY;  document.body.style.position = "fixed";  document.body.style.top = `-${scrollBeforeOpen}px`;  document.body.style.left = "0";  document.body.style.right = "0";}function unlockScroll() {  document.body.style.position = "";  document.body.style.top = "";  window.scrollTo(0, scrollBeforeOpen);}function openTikTokMode(withSound = false) {  lockScroll();  player.classList.add("active");  playerTitle.textContent = currentTitle || "";  playerDesc.textContent = currentDesc || "";  bgVideo.src = currentTrailerUrl;  mainVideo.src = currentTrailerUrl;  bgVideo.muted = true;  if (withSound) {    mainVideo.muted = false;    mainVideo.volume = 1;    soundBtn.textContent = "🔊 Som";  } else {    mainVideo.muted = true;    soundBtn.textContent = "🔇 Som";  }  bgVideo.play().catch(()=>{});  mainVideo.play().catch(()=>{});  setTimeout(() => {    backBtn.focus();  }, 100);}function closeTikTokMode() {  player.classList.remove("active");  bgVideo.pause();  mainVideo.pause();  bgVideo.src = "";  mainVideo.src = "";  unlockScroll();}// overlay buttonsbackBtn.addEventListener("click", closeTikTokMode);soundBtn.addEventListener("click", () => {  mainVideo.muted = !mainVideo.muted;  if (mainVideo.muted) {    soundBtn.textContent = "🔇 Som";  } else {    soundBtn.textContent = "🔊 Som";    mainVideo.volume = 1;  }});watchNowBtn.addEventListener("click", () => {  window.location.href = `./watch.html?tmdb=${currentTmdbId}`;});detailsBtn.addEventListener("click", () => {  window.location.href = `./details.html?tmdb=${currentTmdbId}`;});document.addEventListener("keydown", (e) => {  if (e.key === "Escape" && player.classList.contains("active")) {    closeTikTokMode();  }});// Detectar mobilefunction isMobile() {  return ("ontouchstart" in window) || navigator.maxTouchPoints > 0;}// =====================// Render catálogo por categoria// =====================function createCategorySection(categoryName, items) {  const section = document.createElement("section");  section.className = "category";  const title = document.createElement("h2");  title.className = "category-title";  title.textContent = categoryName || "Sem categoria";  const grid = document.createElement("div");  grid.className = "grid";  items.forEach(movie => {    grid.appendChild(createCard(movie));  });  section.appendChild(title);  section.appendChild(grid);  return section;}function createCard(movie) {  const card = document.createElement("div");  card.className = "card focusable";  card.tabIndex = 0;  card.dataset.tmdb = movie.tmdb_id || "";  card.dataset.title = movie.titulo || "";  card.dataset.desc = movie.description || "Sem descrição disponível.";  card.dataset.trailer = movie.trailer || "";  card.dataset.poster = movie.poster || "";  card.dataset.year = movie.year || "";  card.dataset.genre = movie.genre || "";  card.innerHTML = `    <img class="poster" src="${movie.poster || ""}" alt="poster">    <video class="trailer" muted playsinline preload="metadata"></video>    <div class="glass-action" tabindex="0">▶ Ver trailer com som</div>    <div class="overlay-info">      <p class="title">${movie.titulo || ""}</p>      <p class="subtitle">${movie.year || ""} • ${movie.genre || ""}</p>    </div>  `;  setupPreviewSystem(card);  return card;}// =====================// Preview system// =====================function setupPreviewSystem(card) {  let timer;  let placeholder = null;  const video = card.querySelector(".trailer");  const glassBtn = card.querySelector(".glass-action");  function adjustPositionToScreen(rect) {    let left = rect.left;    let top = rect.top;    const previewWidth = 260;    const previewHeight = Math.round((previewWidth * 16) / 9);    if (left + previewWidth > window.innerWidth - 10) {      left = window.innerWidth - previewWidth - 10;    }    if (left < 10) left = 10;    if (top + previewHeight > window.innerHeight - 10) {      top = window.innerHeight - previewHeight - 10;    }    if (top < 10) top = 10;    return { left, top };  }  function makeFloating() {    const rect = card.getBoundingClientRect();    const fixedPos = adjustPositionToScreen(rect);    placeholder = document.createElement("div");    placeholder.className = "placeholder";    placeholder.style.width = rect.width + "px";    placeholder.style.height = rect.height + "px";    card.parentNode.insertBefore(placeholder, card);    card.style.position = "fixed";    card.style.top = fixedPos.top + "px";    card.style.left = fixedPos.left + "px";  }  function restoreFromFloating() {    if (placeholder) {      placeholder.parentNode.insertBefore(card, placeholder);      placeholder.remove();      placeholder = null;    }    card.style.position = "";    card.style.top = "";    card.style.left = "";  }  function startPreview() {    timer = setTimeout(() => {      makeFloating();      card.classList.add("focused");      const trailerUrl = card.dataset.trailer;      if (trailerUrl && !video.src) {        video.src = trailerUrl;      }      video.currentTime = 0;      video.muted = true;      video.play().catch(()=>{});    }, 250);  }  function stopPreview() {    clearTimeout(timer);    card.classList.remove("focused");    video.pause();    video.currentTime = 0;    restoreFromFloating();  }  // Desktop hover/focus  if (!isMobile()) {    card.addEventListener("mouseenter", startPreview);    card.addEventListener("mouseleave", stopPreview);    card.addEventListener("focus", startPreview);    card.addEventListener("blur", stopPreview);  }  // Click/Enter abre overlay  function openFromCard(withSound) {    currentTmdbId = card.dataset.tmdb;    currentTitle = card.dataset.title;    currentDesc = card.dataset.desc;    currentTrailerUrl = card.dataset.trailer;    openTikTokMode(withSound);  }  // Mobile: primeiro toque abre overlay  card.addEventListener("click", () => {    if (isMobile()) {      openFromCard(false);      return;    }    openFromCard(false);  });  // Botão vidro abre overlay com som  glassBtn.addEventListener("click", (e) => {    e.stopPropagation();    openFromCard(true);  });  // Enter abre overlay (TV/desktop)  card.addEventListener("keydown", (e) => {    if (e.key === "Enter") {      openFromCard(false);    }  });}// =====================// Navegação TV por setas (foco direcional)// =====================function getFocusableElements() {  if (player.classList.contains("active")) {    return Array.from(player.querySelectorAll(".focusable"));  }  return Array.from(document.querySelectorAll(".focusable"));}function findClosestElement(current, direction) {  const focusables = getFocusableElements();  const currentRect = current.getBoundingClientRect();  let best = null;  let bestScore = Infinity;  focusables.forEach(el => {    if (el === current) return;    const rect = el.getBoundingClientRect();    const dx = rect.left - currentRect.left;    const dy = rect.top - currentRect.top;    // Filtrar por direção    if (direction === "right" && rect.left <= currentRect.left) return;    if (direction === "left" && rect.left >= currentRect.left) return;    if (direction === "down" && rect.top <= currentRect.top) return;    if (direction === "up" && rect.top >= currentRect.top) return;    const distance = Math.sqrt(dx*dx + dy*dy);    if (distance < bestScore) {      bestScore = distance;      best = el;    }  });  return best;}document.addEventListener("keydown", (e) => {  const active = document.activeElement;  if (!active || !active.classList.contains("focusable")) return;  if (e.key === "ArrowRight") {    const next = findClosestElement(active, "right");    if (next) next.focus();    e.preventDefault();  }  if (e.key === "ArrowLeft") {    const next = findClosestElement(active, "left");    if (next) next.focus();    e.preventDefault();  }  if (e.key === "ArrowDown") {    const next = findClosestElement(active, "down");    if (next) next.focus();    e.preventDefault();  }  if (e.key === "ArrowUp") {    const next = findClosestElement(active, "up");    if (next) next.focus();    e.preventDefault();  }  if (e.key === "Enter") {    active.click();    e.preventDefault();  }});// =====================// Load catalog// =====================async function loadCatalog() {  const main = document.getElementById("catalog");  const res = await fetch(`${API_BASE}/catalog`);  const data = await res.json();  const grouped = {};  data.forEach(item => {    const cat = item.category || "Sem categoria";    if (!grouped[cat]) grouped[cat] = [];    grouped[cat].push(item);  });  main.innerHTML = "";  Object.keys(grouped).forEach(catName => {    main.appendChild(createCategorySection(catName, grouped[catName]));  });  // focar primeiro card automaticamente em smart tv  const firstCard = document.querySelector(".card");  if (firstCard && !isMobile()) {    firstCard.focus();  }}loadCatalog();
