// Tipos completos para as tabelas do Supabase - Alinhados com estrutura real
// Nota: Tabelas cinema e series foram removidas do sistema

// Placeholder para manter o arquivo válido
export interface DatabasePlaceholder {
  id: string;
}
